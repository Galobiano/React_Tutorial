import Card from './Card.jsx'

function App() {
  
     return(
        <>
        <Card/>
        </>
     );
   
}

export default App

// props (properties) - shared between component 
// propsTypes = that ensures that the passed value is of the correct datatype
// defaultProps = case they are not passed from the parent component 

// conditional rendering - allow to control what gets rendered in your application based on certain condition
//  (show, hide, or change component)
